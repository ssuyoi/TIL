# 2026-01-18: Java 지역 클래스와 익명 클래스 (Local & Anonymous Class)

- 📚 **강의**: 김영한의 자바 중급1 섹션 9

오늘은 클래스 내부에 정의되는 **지역 클래스**와, 이를 더 간결하게 줄인 **익명 클래스**에 대해 학습했다. 이들은 주로 특정 메서드 안에서만 사용되거나, 콜백 패턴을 구현할 때 유용하다.

## 1. 지역 클래스 (Local Class)

메서드 내부에서 정의되는 클래스다. 지역 변수처럼 코드 블록 안에서만 유효하다.

### 변수 캡처 (Variable Capture)
지역 클래스 인스턴스는 메서드가 종료된 후에도 살아있을 수 있다. 하지만 지역 변수는 메서드가 끝나면 스택 프레임과 함께 사라진다. 이 **생명 주기 불일치**를 해결하기 위해 자바는 **변수 캡처**라는 기술을 사용한다.

- **원리**: 인스턴스를 생성할 때, 사용하는 지역 변수의 값을 복사해서 인스턴스 내부에 보관한다.
- **제약**: 캡처되는 지역 변수는 **사실상 파이널(Effectively Final)**이어야 한다. 즉, 값이 변경되면 안 된다. (데이터 동기화 문제 방지)

## 2. 익명 클래스 (Anonymous Class)

지역 클래스를 사용하다 보면, 이름이 굳이 필요 없고 선언과 동시에 딱 한 번만 생성해서 사용하는 경우가 많다. 이럴 때 **익명 클래스**를 사용한다.

```java
// 인터페이스 구현과 객체 생성을 동시에!
Printer printer = new Printer() {
    @Override
    public void print() {
        System.out.println("출력합니다.");
    }
};
```

- **특징**:
    - 이름이 없다.
    - `new 인터페이스/클래스() { ... }` 문법을 사용한다.
    - 상위 클래스를 상속받거나 인터페이스를 구현하면서 동시에 인스턴스를 생성한다.

## 3. 활용: 콜백 패턴

익명 클래스는 **전략 패턴(Strategy Pattern)**이나 **템플릿 콜백 패턴**에서 코드 조각(메서드)을 파라미터로 넘겨줄 때 매우 유용하다.

```java
// 코드 조각(비즈니스 로직)을 인자로 전달
hello(new Processor() {
    @Override
    public void run() {
        System.out.println("비즈니스 로직 실행");
    }
});
```

람다(Lambda)가 등장하기 전까지는 이 방식이 자바에서 함수를 파라미터로 넘기는 유일한 방법이었으며, 지금도 람다의 기반 개념으로 중요하게 사용된다.
