# 2026-01-16: Java 래퍼 클래스(Wrapper Class)와 Class 클래스

- 📚 **강의**: 김영한의 자바 중급1 섹션 5

오늘은 기본형(Primitive Type)을 객체로 다루기 위한 **래퍼 클래스**와, 실행 중인 클래스의 메타데이터를 다루는 **Class 클래스**에 대해 학습했다.

## 1. 래퍼 클래스 (Wrapper Class)

자바에는 `int`, `double` 같은 기본형이 있지만, 이들은 객체가 아니기 때문에 다음과 같은 **한계**가 있다.
- 메서드를 가질 수 없다. (예: `int.toString()` 불가)
- 컬렉션(`List`, `Map` 등)에 저장할 수 없다. (`List<int>` 불가)
- `null` 값을 가질 수 없다. (값 부재 표현 불가)

이러한 문제를 해결하기 위해 **기본형을 객체로 감싸서(Wrap) 만든 클래스**가 바로 래퍼 클래스다. (`Integer`, `Long`, `Double` 등)
- `String`과 마찬가지로 **불변 객체**로 설계되어 있다.

## 2. 오토 박싱 / 오토 언박싱 (Auto Boxing / Unboxing)

과거(Java 5 이전)에는 기본형과 래퍼 클래스 간의 변환을 명시적으로 해주어야 했다.
- **박싱(Boxing)**: `Integer.valueOf(10)` (기본형 -> 객체)
- **언박싱(Unboxing)**: `obj.intValue()` (객체 -> 기본형)

현재는 컴파일러가 이를 자동으로 처리해주는 **오토 박싱/언박싱**을 지원하여, 기본형처럼 편리하게 사용할 수 있다.

```java
// 오토 박싱: int -> Integer 자동 변환
Integer boxed = 10; 

// 오토 언박싱: Integer -> int 자동 변환
int unboxed = boxed;
```

## 3. 성능 비교 (Wrapper vs Primitive)

편의성을 위해 래퍼 클래스를 많이 사용하지만, **성능** 면에서는 기본형이 더 우세하다.

- **기본형 (`int`)**: 메모리에서 단순히 크기(4byte)만큼만 차지하며, 연산 속도가 매우 빠르다.
- **래퍼 클래스 (`Integer`)**: 객체 메타데이터를 포함하므로 메모리를 더 많이 차지하며, 객체 생성 및 참조 과정(오버헤드)이 필요하다.

> **💡 권장 가이드**
> - **대부분의 경우**: 유지보수와 편의성을 위해 **래퍼 클래스**나 오토 박싱을 적극 활용하자. (일반적인 웹 앱 연산에서는 큰 차이 없음)
> - **성능이 치명적인 경우**: 수십억 번의 반복 연산 등 극단적인 성능이 요구될 때만 **기본형**을 사용하여 최적화하자.

## 4. 주요 메서드

- `valueOf(int i)`: `Integer` 객체 반환. **-128 ~ 127** 범위의 숫자는 캐싱(Caching)된 객체를 반환하여 성능을 최적화한다.
- `parseInt(String s)`: 문자열을 기본형 `int`로 변환한다.
- `compareTo(Integer anotherInteger)`: 값의 크기를 비교한다. (같으면 0, 작으면 -1, 크면 1)

## 5. Class 클래스

자바는 실행 중인 클래스의 정보(메타데이터)를 다룰 수 있는 `Class` 클래스를 제공한다.
- 클래스의 이름, 메서드 정보, 필드 정보, 생성자, 부모 클래스, 인터페이스 등을 조회할 수 있다.
- **리플렉션(Reflection)**: `Class` 객체를 통해 실행 중에 코드를 분석하거나, 메서드를 호출하고 객체를 생성할 수 있는 기능.

```java
// Class 객체 획득 방법
Class clazz1 = String.class; // 1. 클래스에서 조회
Class clazz2 = new String().getClass(); // 2. 인스턴스에서 조회
Class clazz3 = Class.forName("java.lang.String"); // 3. 문자열로 조회
```
