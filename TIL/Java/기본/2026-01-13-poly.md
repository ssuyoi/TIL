# 2026-01-13: Java 다형성(Polymorphism) 학습

- 📚 **강의**: 김영한의 자바 기본 섹션 11,12

오늘은 객체 지향 프로그래밍의 꽃이라 불리는 **다형성**에 대해 학습했다. 다형성의 기본 원리부터 시작하여, 추상 클래스와 인터페이스를 도입하며 코드가 어떻게 유연해지는지(OCP 원칙 준수) 단계별로 확인했다.

## 1. 다형성 기초

다형성은 **"다양한 형태를 참조할 수 있고, 다양한 실행 결과를 낼 수 있는 능력"**을 말한다. 이를 위해 두 가지 핵심 개념이 필요하다.

### 1) 다형적 참조
부모 타입의 변수가 자식 인스턴스를 참조할 수 있다.
```java
Parent poly = new Child(); // 부모는 자식을 품을 수 있다.
```
- **업캐스팅(Upcasting)**: 자식 타입을 부모 타입으로 변경하는 것. 자동으로 이루어진다.
- **다운캐스팅(Downcasting)**: 부모 타입을 자식 타입으로 변경하는 것. 명시적 형변환이 필요하다. (`(Child) poly`)

### 2) 메서드 오버라이딩 우선권
다형성을 이해하는 가장 중요한 규칙이다.
**"참조 변수의 타입과 상관없이, 오버라이딩된 메서드가 항상 우선권을 가진다."**

```java
Parent poly = new Child();
poly.method(); // Child의 method()가 실행됨!
```
변수 `poly`는 `Parent` 타입이지만, 실제 메모리 상의 인스턴스는 `Child`이다. 자바는 메서드를 호출할 때 실제 인스턴스에 오버라이딩된 메서드가 있는지 확인하고, 있다면 그것을 최우선으로 실행한다.

## 2. 다형성 활용 (동물 소리 예제 Evolution)

동물의 소리를 듣는 기능을 구현하며 다형성의 필요성을 느꼈다.

### 문제 상황 (다형성 X)
`Dog`, `Cat`, `Caw` 클래스가 서로 다른 타입이기 때문에, 하나의 배열에 담거나 하나의 메서드로 처리할 수 없다. 코드가 중복되고 새로운 동물이 추가될 때마다 수정해야 한다.

### 1단계: 상속 도입 (`Animal` 상속)
모든 동물이 `Animal`을 상속받게 하여 타입을 하나로 통일했다.

```java
public class Animal {
    public void sound() {
        System.out.println("동물 울음 소리");
    }
}
public class Dog extends Animal {
    @Override
    public void sound() {
        System.out.println("멍멍");
    }
}
```

- **장점**: `Animal[]` 배열에 다양한 동물을 담아 일괄 처리(`sound()` 호출)할 수 있다.
- **아쉬운 부분**:
    - `Animal` 클래스를 직접 생성(`new Animal()`)할 수 있다. (실제하지 않는 동물이 생성됨)
    - 자식 클래스에서 `sound()` 메서드 오버라이딩을 깜빡해도 컴파일 오류가 나지 않는다. (부모의 기능이 실행됨)

### 2단계: 추상 클래스 (`abstract class`)
`Animal` 클래스를 직접 생성할 일은 없으므로 추상 클래스로 선언했다.

```java
public abstract class Animal {
    public abstract void sound(); // 추상 메서드
    
    public void move() { // 상속받아 사용할 공통 기능
        System.out.println("동물이 이동합니다.");
    }
}
```

- **해결**:
    - `abstract` 키워드로 인해 `new Animal()` 호출 시 컴파일 오류가 발생한다. (생성 방지)
    - 추상 메서드(`abstract void sound()`)는 자식 클래스에서 **반드시 오버라이딩**해야 하므로 실수를 방지한다.
- **아쉬운 부분**:
    - 자바는 **단일 상속**만 지원한다. 만약 다른 부모를 상속받아야 하는 상황이라면 `Animal`을 상속받을 수 없다.

### 3단계: 인터페이스 (`interface`)
상속보다 더 유연한 구조가 필요할 때 사용한다.

```java
public interface InterfaceAnimal {
    void sound(); // public abstract 생략
    void move();
}

public class Dog implements InterfaceAnimal { ... }
```

- **특징**:
    - 모든 메서드가 `public abstract`인 순수 추상 클래스와 같다.
    - **다중 구현**을 지원한다. (`implements A, B`)
- **장점**:
    - 상속의 제약(단일 상속)에서 자유롭다.
    - "이 기능을 구현했다"는 **계약(약속)**의 성격이 강하다.

## 3. 좋은 객체 지향 설계 (OCP)

다형성을 잘 활용하면 **OCP (Open-Closed Principle, 개방-폐쇄 원칙)**를 지킬 수 있다.

- **Open for extension**: 새로운 기능(새로운 동물 `Pig`, `Chicken`)을 추가할 때, 기존 코드를 변경하지 않고 확장할 수 있다.
- **Closed for modification**: 클라이언트 코드(동물 소리를 호출하는 `soundAnimal` 메서드)는 새로운 동물이 추가되어도 코드를 수정할 필요가 없다.

> **"역할(인터페이스)과 구현(클래스)을 분리하라."**
> 클라이언트는 대상의 역할(인터페이스)만 알면 된다. 내부 구현이 변경되어도 클라이언트에 영향을 주지 않는다.
