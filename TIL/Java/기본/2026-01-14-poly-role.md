# 2026-01-14: Java 다형성 활용 (역할과 구현)

- 📚 **강의**: 김영한의 자바 기본 섹션 13

오늘은 다형성을 활용하여 **역할(Role)**과 **구현(Implementation)**을 분리하는 설계 원칙에 대해 학습했다. 클라이언트 코드를 변경하지 않고도 서버(구현) 기능을 유연하게 변경/확장할 수 있는 OCP 원칙의 중요성을 확인했다.

## 1. 역할과 구현의 분리

객체 지향 설계의 핵심은 클라이언트가 **대상의 구현이 아닌 역할(인터페이스)에 의존**하게 하는 것이다.
- **역할**: 인터페이스 (예: 자동차 역할)
- **구현**: 인터페이스를 구현한 클래스 (예: K3, 아반떼, 모델3)
- **효과**: 클라이언트는 내부 구현이 바뀌거나 새로운 구현이 추가되어도 영향을 받지 않는다.

## 2. 구체적인 의존 관계 문제

인터페이스 없이 구체적인 클래스에 직접 의존할 때 발생하는 문제를 확인했다.

```java
public class Driver {
    private K3Car k3Car;
    private Model3Car model3Car; // 새로운 차가 추가될 때마다 필드와 메서드가 계속 추가되어야 함
    
    public void drive() {
        if (k3Car != null) k3Car.startEngine();
        else if (model3Car != null) model3Car.startEngine();
    }
}
```

- **문제점**:
    - **강한 결합도**: `Driver`가 구체적인 차종(`K3Car`, `Model3Car`)을 모두 알고 있어야 한다.
    - **확장성 부족**: 새로운 차가 나올 때마다 `Driver` 코드를 수정해야 한다. (OCP 위반)

## 3. 인터페이스 도입과 OCP

`Car` 인터페이스를 도입하여 의존 관계를 개선했다.

```java
public class Driver {
    private Car car; // 구체적인 차종 대신 인터페이스(역할)에 의존

    public void setCar(Car car) {
        this.car = car;
    }

    public void drive() {
        car.startEngine(); // 어떤 차가 들어오든 동일한 메서드 호출
    }
}
```

- **OCP (Open-Closed Principle) 준수**:
    - **Open for extension**: 새로운 차종(`NewCar`)을 얼마든지 추가할 수 있다.
    - **Closed for modification**: 새로운 차가 추가되어도 `Driver`의 코드는 **전혀 변경할 필요가 없다.**
- `Driver`는 이제 "자동차의 역할"만 알면 되며, 실제로 어떤 차가 움직이는지는 몰라도 된다.

## 4. 실습 예제 (Sender)

다형성을 활용하여 이메일, SMS, 페이스북 등 다양한 방식으로 메시지를 전송하는 시스템을 구현했다.

```java
public interface Sender {
    void sendMessage(String message);
}

// 다형성을 활용한 일괄 처리
Sender[] senders = {new EmailSender(), new SmsSender(), new FaceBookSender()};
for (Sender sender : senders) {
    sender.sendMessage("환영합니다!");
}
```

- 새로운 전송 방식(예: `KakaoSender`)이 추가되어도, 이를 호출하는 클라이언트 코드는 변경할 필요 없이 배열에 객체만 추가하면 된다.
