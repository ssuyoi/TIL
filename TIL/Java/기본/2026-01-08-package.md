# 2026-01-08: Java 패키지(Package) 학습

- 📚 **강의**: 김영한의 자바 기본 섹션 6

오늘은 클래스를 효율적으로 관리하기 위한 **패키지(Package)**에 대해 학습했다. 패키지의 개념과 필요성, 그리고 다른 패키지의 클래스를 사용하는 방법(`import`)을 익혔다.

## 1. 패키지란?

클래스를 기능별로 묶어서 관리하는 컨테이너다.
마치 컴퓨터의 폴더(디렉토리)와 같다. 수많은 파일이 한 폴더에 있으면 관리하기 힘들듯이, 수많은 클래스도 패키지로 분류해야 관리가 용이하다.

### 역할
- **기능 분류**: 관련된 기능의 클래스들을 묶어둔다.
- **이름 충돌 방지**: 패키지가 다르면 같은 이름의 클래스를 만들 수 있다.

## 2. import

다른 패키지에 있는 클래스를 사용하려면 `import` 키워드를 사용해야 한다.

- **사용 방법**: `package` 선언 바로 아래에 `import 패키지명.클래스명;` 형식으로 작성한다.
- **편의 기능**: `import 패키지명.*;` 을 사용하면 해당 패키지의 모든 클래스를 사용할 수 있다.

> **참고**: 같은 패키지에 있는 클래스는 `import` 없이 바로 사용할 수 있다.

## 3. 클래스 이름 중복

서로 다른 패키지에 있지만 이름이 같은 클래스를 동시에 사용해야 하는 경우가 있다.
이때는 하나만 `import` 할 수 있고, 나머지는 **전체 패키지 경로(Full Qualified Name)**를 적어줘야 한다.

```java
import basic.pack.a.User; // 자주 사용하는 것을 import

public class PackageMain {
    public static void main(String[] args) {
        User userA = new User(); // import한 클래스
        basic.pack.b.User userB = new basic.pack.b.User(); // 전체 경로 사용
    }
}
```
