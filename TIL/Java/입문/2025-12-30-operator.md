# 2025-12-30: Java 연산자 (Operator) 학습

- 📚 **강의**: 김영한의 자바 입문 섹션 4

오늘은 Java의 다양한 연산자들과 그 사용법에 대해 학습했다. 연산자 우선순위부터 증감, 비교, 논리, 대입 연산자까지 다양한 예제를 통해 실습했다.

## 1. 연산자 우선순위

수학 연산과 마찬가지로 Java에서도 연산자 간에 우선순위가 존재한다.
- `*`, `/` 가 `+`, `-` 보다 우선순위가 높다.
- 연산자 우선순위가 헷갈리거나 코드의 명확성을 높이고 싶다면 **괄호 `()`**를 사용하는 것이 좋다.

```java
int sum1 = 1 + 2 * 3; // 1 + 6 = 7
int sum2 = (1 + 2) * 3; // 3 * 3 = 9
```

## 2. 증감 연산자

변수의 값을 1 증가시키거나 감소시킬 때 사용하는 연산자이다. 위치에 따라 동작 방식이 다르다.

- **전위 증감 연산자 (`++a`)**: 코드 라인이 실행되기 전에 먼저 값을 증가시킨다.
- **후위 증감 연산자 (`a++`)**: 코드 라인이 실행된 후에 값을 증가시킨다.

```java
// 전위
int a = 1;
int b = ++a; // a를 2로 증가시킨 후 b에 대입 (a=2, b=2)

// 후위
a = 1;
b = a++; // b에 a(1)를 대입하고 나서 a를 2로 증가 (a=2, b=1)
```

## 3. 비교 연산자

### 숫자 비교
두 값을 비교하여 참(`true`) 또는 거짓(`false`)을 반환한다.
- `==`: 같다
- `!=`: 다르다
- `>`, `<`, `>=`, `<=`: 크기 비교

```java
int a = 2;
int b = 3;
System.out.println(a == b); // false
System.out.println(a != b); // true
System.out.println(a < b);  // true
```

### 문자열 비교
문자열(String)을 비교할 때는 `==` 대신 **`.equals()`** 메서드를 사용해야 한다.
- `==`는 객체의 참조값(메모리 주소)을 비교하므로, 내용이 같아도 `false`가 나올 수 있다.
- `.equals()`는 문자열의 실제 내용을 비교한다.

```java
String str1 = "hello";
String str2 = new String("hello"); // 새로운 객체 생성 가정

boolean result1 = str1.equals("hello"); // true (권장)
boolean result2 = (str1 == "hello");    // 의도와 다르게 동작할 수 있음
```

## 4. 논리 연산자 

여러 조건식을 결합하거나 논리를 뒤집을 때 사용한다.

- **`&&` (AND)**: 두 조건이 모두 참이어야 참.
- **`||` (OR)**: 두 조건 중 하나라도 참이면 참.
- **`!` (NOT)**: 참을 거짓으로, 거짓을 참으로 바꿈.

```java
boolean a = true;
boolean b = false;

System.out.println(a && b); // false
System.out.println(a || b); // true
System.out.println(!a);     // false

// 범위 체크 예시
int score = 15;
boolean result = score > 10 && score < 20; // true
```

## 5. 대입 연산자

변수에 값을 대입하면서 동시에 연산을 수행한다.
- `+=`, `-=`, `*=`, `/=`, `%=`

```java
int a = 10;
a += 5; // a = a + 5; (15)
a -= 2; // a = a - 2; (13)
```

## 6. 실습 예제

### 합계와 평균 구하기
```java
int num1 = 10, num2 = 20, num3 = 30;
int sum = num1 + num2 + num3;
int average = sum / 3; // 정수 나눗셈 주의
System.out.println(sum + ", " + average);
```
double 형으로 계산하면 소수점까지 표현 가능하다.

### 점수 범위 확인하기
점수가 80점 이상 100점 이하인지 확인하는 로직.
```java
int score = 80;
boolean result = score >= 80 && score <= 100;
System.out.println(result); // true
```
