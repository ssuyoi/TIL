# 2026-01-29: 애노테이션 (Annotation)

- 📚 **강의**: 김영한의 자바 고급2 섹션 14

오늘은 코드에 부가 정보(메타데이터)를 남기는 **애노테이션(Annotation)**의 개념과 정의 방법, 그리고 이를 활용해 **중복 검증 로직을 제거하는 방법**에 대해 학습했다.

## 1. 애노테이션이란?

코드에 붙이는 **"주석"**이자 **"메타데이터"**다.
소스코드 자체에는 직접적인 영향을 주지 않지만, 컴파일러나 런타임에 처리기(Processor)가 이를 읽어서 코드를 생성하거나 특정 동작을 수행하게 할 수 있다.

-   **`@Override`**: 컴파일러에게 "이거 부모 메서드 재정의한 거야, 체크해줘"라고 알림.
-   **`@Deprecated`**: "이거 이제 쓰지 마"라고 경고.

## 2. 애노테이션 정의 (메타 애노테이션)

커스텀 애노테이션을 만들 때는, 이 애노테이션이 **언제까지 유지**될지, **어디에 붙일지**를 결정하는 "메타 애노테이션"이 필요하다.

```java
@Retention(RetentionPolicy.RUNTIME) // 런타임까지 살아있어야 리플렉션으로 읽을 수 있음 (필수!)
@Target(ElementType.FIELD)          // 필드에만 붙일 수 있음
@Documented                         // Javadoc에 포함시킴
public @interface NotEmpty {
    String message() default "값이 비어있습니다.";
}
```

### 2-1. 주요 메타 애노테이션
메타 애노테이션은 "애노테이션을 위한 애노테이션"이다.

#### 1) @Retention (생명 주기)
애노테이션이 언제까지 살아있을지 결정한다.
-   **SOURCE**: 소스 코드에만 존재. 컴파일 시 사라짐. (예: `@Override`, 리롬복 `@Getter`)
-   **CLASS** (기본값): 클래스 파일(.class)까지는 남지만, 런타임에 JVM이 무시함. (리플렉션 X)
-   **RUNTIME**: 런타임까지 유지됨. 리플렉션으로 조회 가능. **(실무에서 가장 많이 사용)**

#### 2) @Target (적용 위치)
애노테이션을 붙일 수 있는 위치를 제한한다.
-   `ElementType.TYPE`: 클래스, 인터페이스, 열거형
-   `ElementType.FIELD`: 필드
-   `ElementType.METHOD`: 메서드
-   `ElementType.PARAMETER`: 파라미터
-   `ElementType.CONSTRUCTOR`: 생성자
-   `ElementType.LOCAL_VARIABLE`: 지역 변수

#### 3) @Documented
이 애노테이션을 붙이면, 자바독(Javadoc) 생성 시 문서에 포함된다. (라이브러리 만들 때 유용)

### 2-2. 정의 규칙 및 가능한 데이터 타입
애노테이션에 정의할 수 있는 타입은 매우 제한적이다.
-   **가능**: 기본 타입(int, boolean...), String, Class, Enum, Annotation, 그리고 이들의 배열(`[]`).
-   **불가능**: 일반 클래스 객체 (`User`, `Member` 등 불가), `void` 반환 타입.
-   **특징**: 매개변수를 가질 수 없다 (`()` 형태).

### 2-2. 상속 (`@Inherited`)
-   이 메타 애노테이션을 붙이면, **자식 클래스**도 부모 클래스의 애노테이션을 물려받는다.
-   **주의**: **인터페이스 구현체**에는 상속되지 않는다. (인터페이스는 상태를 가지지 않으므로 논리적으로 맞지 않음)

### 2-3. 자바 기본 제공 애노테이션
-   `@Override`: 메서드 재정의 확인.
-   `@Deprecated`: 더 이상 사용되지 않음 표시.
-   `@SuppressWarnings`: 컴파일 경고 억제 (예: `"unused"`, `"deprecation"`, `"unchecked"`).

## 3. 실무 예시: 검증기(Validator) 만들기

모든 객체마다 `if (name == null) ...` 검증 로직을 짜는 것은 중복의 연속이다. 애노테이션과 리플렉션을 활용하면 이를 아름답게 해결할 수 있다.

### 3-1. 기존 방식 (하드코딩)
```java
if (user.getName() == null) throw ...
if (user.getAge() < 1) throw ...
// 객체마다 일일이 검증 로직 작성 필요
```

### 3-2. 애노테이션 방식 (범용 검증기)
**`Validator` 클래스 하나**만 잘 만들어두면, 어떤 객체든 애노테이션만 붙여서 검증할 수 있다.

```java
public class User {
    @NotEmpty(message = "이름은 필수입니다.")
    private String name;

    @Range(min = 1, max = 100)
    private int age;
}

// 사용
Validator.validate(user); // 자동으로 모든 필드의 애노테이션을 읽어서 검증 수행!
```

> **💡 핵심**:
> 로직은 **Validator**(처리용 로직)에 위임하고, 객체에는 **애노테이션**(데이터)만 남긴다.
> 코드가 훨씬 깔끔해지고, 검증 로직을 재사용할 수 있게 된다. (Spring의 `@Valid` 원리)
