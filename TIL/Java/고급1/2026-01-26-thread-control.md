# 2026-01-26: 스레드 제어 (State, Join)

- 📚 **강의**: 김영한의 자바 고급1 섹션 4

오늘은 스레드의 상태 변화(**생명주기**)와, 다른 스레드가 끝날 때까지 기다리는 **`join()`** 메서드에 대해 학습했다.

## 1. 스레드의 생명주기 (Life Cycle)

자바 스레드는 6가지 상태를 가진다. `thread.getState()`로 확인할 수 있다.

| 상태 | 설명 |
| :--- | :--- |
| **NEW** | 스레드가 생성되었지만, 아직 `start()`가 호출되지 않은 상태 |
| **RUNNABLE** | 실행 중이거나, 실행 대기 상태 (CPU 자원을 기다림) |
| **BLOCKED** | 동기화 블록(Lock)에 진입하기 위해 대기하는 상태 |
| **WAITING** | 다른 스레드가 통지(notify)하거나 종료되길 기다리는 상태 (`join`, `wait`) |
| **TIMED_WAITING** | 정해진 시간 동안 기다리는 상태 (`sleep(ms)`, `join(ms)`) |
| **TERMINATED** | 실행이 완료되어 종료된 상태 |

## 2. join() - 다른 스레드 기다리기

멀티스레드 프로그래밍에서 흔한 문제는, **메인 스레드가 워커 스레드보다 먼저 종료되어** 결과를 제대로 받지 못하는 것이다. 이때 `join()`을 사용한다.

### 문제 상황
```java
Thread thread = new Thread(task);
thread.start();
// 여기서 task의 계산 결과를 쓰고 싶은데, thread가 아직 안 끝났을 수 있음!
System.out.println(task.result); // 0 (오류)
```

### 해결: join() 사용
호출한 스레드(여기선 main)가 대상 스레드(`thread`)가 **TERMINATED** 상태가 될 때까지 기다린다. 대기하는 동안 main 스레드는 **WAITING** 상태가 된다.

```java
Thread thread = new Thread(task);
thread.start();

try {
    thread.join(); // 메인 스레드는 thread가 끝날 때까지 여기서 멈춤 (WAITING)
} catch (InterruptedException e) {
    throw new RuntimeException(e);
}

System.out.println(task.result); // 정상 결과 출력
```

> **주의**: `join()`은 예외 처리(`InterruptedException`)가 필수다.
