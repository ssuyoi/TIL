# 2026-01-23: Java Map 구현체 (Map)

- 📚 **강의**: 김영한의 자바 중급2 섹션 10

오늘은 Key-Value 쌍으로 데이터를 저장하는 **Map** 인터페이스와 주요 구현체들의 특징을 학습했다.

## 1. Java Map 구현체 비교

`Map`은 키(Key)의 유일성을 보장하며, 키를 통해 값(Value)을 매우 빠르게 조회할 수 있다. `Set`과 마찬가지로 내부 구조에 따라 3가지 주요 구현체로 나뉜다.

| 구현체 | 특징 | 입력 순서 보장 | 키 정렬 | 성능 |
| :--- | :--- | :--- | :--- | :--- |
| **HashMap** | 해시 알고리즘 사용. 가장 일반적. | ❌ | ❌ | **O(1)** (가장 빠름) |
| **LinkedHashMap** | 해시 + 연결 리스트. 입력한 순서대로 저장됨. | ✅ | ❌ | **O(1)** (HashMap보다 약간 느림) |
| **TreeMap** | 이진 탐색 트리(Red-Black Tree). 키 값에 따라 정렬됨. | ❌ | ✅ | **O(log N)** (범위 검색, 정렬 필요 시) |

## 2. 사용 예제

```java
public class JavaMapMain {
    public static void main(String[] args) {
        // 1. HashMap: 순서 보장 X, 빠름
        run(new HashMap<>()); 
        
        // 2. LinkedHashMap: 입력 순서 보장
        run(new LinkedHashMap<>());
        
        // 3. TreeMap: 키 순서대로 정렬 (1, 2, A, B, C...)
        run(new TreeMap<>());
    }

    private static void run(Map<String, Integer> map) {
        System.out.println("map = " + map.getClass());
        // 데이터 저장
        map.put("C", 10);
        map.put("B", 20);
        map.put("A", 30);
        map.put("1", 40);
        map.put("2", 50);

        // 키 목록 조회 및 순회
        Set<String> keySet = map.keySet();
        Iterator<String> iterator = keySet.iterator();
        while (iterator.hasNext()) {
            String key = iterator.next();
            System.out.println(key + " : " + map.get(key));
        }
    }
}

```

## 3. Map 주요 메서드

`Map` 인터페이스가 제공하는 공통 기능들이다.

### 주요 메서드 요약

| 분류 | 메서드 | 설명 | 비고 |
| :--- | :--- | :--- | :--- |
| **데이터 조작** | `put(key, value)` | 키와 값을 저장 (없으면 추가, 있으면 수정) | 수정 시 이전 값 반환 |
| | `putIfAbsent(key, value)` | 키가 없을 때만 저장 | 이미 있으면 무시 |
| | `get(key)` | 키로 값을 조회 | 없으면 `null` |
| | `getOrDefault(key, default)` | 키로 값을 조회하되, 없으면 기본값 반환 | **매우 유용** |
| | `remove(key)` | 키와 매핑된 값을 삭제 | |
| **데이터 확인** | `containsKey(key)` | 키가 있는지 확인 | **O(1)** |
| | `containsValue(value)` | 값이 있는지 확인 | **O(N)** |
| | `isEmpty()` / `size()` | 비어있는지 / 크기 확인 | |
| **순회 (Views)** | `keySet()` | 모든 키를 `Set`으로 반환 | |
| | `values()` | 모든 값을 `Collection`으로 반환 | |
| | `entrySet()` | 키-값 쌍(`Entry`)을 `Set`으로 반환 | **가장 효율적** |

### 컬렉션 뷰 (Collection Views)
Map은 `Iterator`를 직접 지원하지 않으므로, 3가지 반환 메서드를 통해 순회한다.
1. `keySet()`: 모든 **키(Key)**만 Set으로 반환.
2. `values()`: 모든 **값(Value)**만 Collection으로 반환.
3. `entrySet()`: **키-값 쌍(Entry)**을 Set으로 반환. (가장 효율적인 순회 방법)

```java
// entrySet()을 이용한 효율적인 순회
for (Map.Entry<String, Integer> entry : map.entrySet()) {
    String key = entry.getKey();
    Integer value = entry.getValue();
    System.out.println(key + "=" + value);
}
```
> **💡 실무 활용 가이드**:
> 1.  **`HashMap` (기본)**: 실무에서 **90% 이상**은 이것을 사용한다. 순서가 중요하지 않다면 무조건 성능이 가장 좋은 `HashMap`을 선택하자.
> 2.  **`LinkedHashMap`**: 데이터의 **입력 순서**를 유지해야 하는 특수한 경우에만 사용한다.
> 3.  **`TreeMap`**: 데이터가 항상 **정렬된 상태**로 유지되어야 하거나, 범위 검색이 필요한 경우에만 사용한다.


## 4. Key 사용 시 주의사항 ⚠️ (필독)

`Map`의 Key로 사용자 정의 객체(예: `Member`)를 사용할 경우, 자료구조의 특성에 맞춰 반드시 재정의해야 하는 메서드들이 있다.

### 4-1. HashMap, LinkedHashMap: `hashCode()`와 `equals()`
이 두 구현체는 **해시(Hash) 알고리즘**을 기반으로 동작한다.
1.  **`hashCode()`**: 데이터를 저장할 **위치(인덱스)**를 결정한다.
    -   이것을 재정의하지 않으면, 같은 데이터를 가진 객체라도 메모리 주소가 달라 서로 다른 위치(버킷)에 저장된다.
2.  **`equals()`**: 같은 위치(해시 충돌)에 있는 데이터 중 **진짜 내 데이터**를 찾는다.
    -   이것을 재정의하지 않으면, `hashCode`가 같아 같은 버킷까지 찾아가더라도, 값이 같은지 비교하지 못해(참조값 비교) 결국 데이터를 찾지 못한다.

> **결론**: `Member` 같은 객체를 Key로 쓰려면, 반드시 **모든 필드를 사용하여 `hashCode()`와 `equals()`를 오버라이딩**해야 한다.

### 4-2. TreeMap: `Comparable` 인터페이스
`TreeMap`은 이진 탐색 트리 구조를 사용하므로, Key들을 **정렬**해서 저장한다. 따라서 **크기 비교(대소 비교)**가 가능해야 한다.

1.  **`Comparable` 구현**: Key로 사용할 클래스(`Member`)가 `Comparable<Member>`를 구현(`compareTo` 오버라이딩)해야 한다.
2.  **`Comparator` 제공**: 만약 클래스를 수정할 수 없다면, `TreeMap` 생성 시점에 별도의 비교자(`new TreeMap<>(comparator)`)를 넣어줘야 한다.

> **결론**: 비교 기준이 없으면 `ClassCastException` 런타임 에러가 발생한다.
