# 2026-01-21: Java 리스트 인터페이스 (List Interface)

- 📚 **강의**: 김영한의 자바 중급2 섹션 6

오늘은 `ArrayList`와 `LinkedList`의 공통 기능을 추상화한 **List 인터페이스**와, 이를 통해 **OCP(Open-Closed Principle)**를 지키는 방법에 대해 학습했다.

## 1. List 인터페이스의 도입

`ArrayList`와 `LinkedList`는 내부 구현은 다르지만 (`배열` vs `노드`), `add`, `get`, `remove` 등 제공하는 기능은 거의 비슷하다.
이 공통점들을 모아 `List`라는 인터페이스를 정의하면, 클라이언트 코드는 구체적인 구현 클래스가 아닌 **추상적인 인터페이스**에 의존할 수 있다.

```java
public interface MyList<E> {
    void add(E e);
    E get(int index);
    // ...
}
```

## 2. 다형성과 OCP (Open-Closed Principle)

`BatchProcessor`라는 클래스가 리스트 데이터를 처리한다고 가정해보자.

```java
public class BatchProcessor {
    // 구체적인 클래스(MyArrayList) 대신 인터페이스(MyList)에 의존
    private final MyList<Integer> list;

    // 생성자 주입 (Dependency Injection)
    public BatchProcessor(MyList<Integer> list) {
        this.list = list;
    }
    
    public void logic(int size) { ... }
}
```

- **OCP 준수**: `BatchProcessor` 코드를 전혀 변경하지 않고도, 외부에서 생성자를 통해 `MyArrayList`를 `MyLinkedList`로 교체할 수 있다.
- **전략 패턴**: `MyList`가 전략 인터페이스가 되고, 실제 리스트 구현체들이 전략이 된다.

## 3. 성능 비교 (Trade-off)

인터페이스를 통해 유연함은 얻었지만, 어떤 구현체를 주입하느냐에 따라 **성능**은 극적으로 달라질 수 있다.
다음은 리스트의 **가장 앞(index 0)**에 데이터를 대량으로 추가하는 시나리오다.

| 주입된 구현체 | 연산 특징 (add(0)) | 반복 시 총 시간 복잡도 (N번 추가) | 성능 |
| :--- | :--- | :--- | :--- |
| **ArrayList** | 데이터를 모두 뒤로 밀어야 함 (`O(N)`) | `N * O(N)` = **`O(N^2)`** | 매우 느림 |
| **LinkedList** | 첫 노드만 변경하면 됨 (`O(1)`) | `N * O(1)` = **`O(N)`** | 매우 빠름 |

> **💡 결론**:
> - 무조건 좋은 자료구조는 없다. 상황(조회 중심 vs 삽입 중심)에 맞춰 적절한 구현체를 선택해야 한다.
> - **인터페이스(`List`)를 사용**하면, 나중에 성능 튜닝이 필요할 때 코드를 거의 고치지 않고 구현체만 바꿔 끼울 수 있다. 이것이 다형성의 힘이다.
