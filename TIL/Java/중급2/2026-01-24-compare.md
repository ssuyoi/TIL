# 2026-01-24: 정렬 (Comparable, Comparator)

- 📚 **강의**: 김영한의 자바 중급2 섹션 11

오늘은 객체를 정렬할 때 사용하는 두 가지 인터페이스 **Comparable**과 **Comparator**의 차이점과 사용법에 대해 학습했다.

## 1. Comparable vs Comparator

자바가 객체를 정렬하려면 "누가 더 큰지" 비교할 수 있어야 한다.

| 구분 | Comparable (유능한 나) | Comparator (제3의 심판) |
| :--- | :--- | :--- |
| **역할** | 객체 **스스로**가 다른 객체와 비교 가능한 상태임을 정의 | 별도의 **외부 클래스**가 두 객체를 비교해줌 |
| **기준** | **기본 정렬 기준** (Natural Ordering) | **특수 정렬 기준** (Custom Ordering) |
| **메서드** | `compareTo(T o)` | `compare(T o1, T o2)` |
| **패키지** | `java.lang` | `java.util` |
| **사용** | `Arrays.sort(arr)` (알아서 정렬) | `Arrays.sort(arr, comparator)` (기준 제공) |

## 2. Comparable: 기본 정렬 기준 정의

클래스 자체가 `Comparable`을 구현하면, "나는 비교 가능한 객체다"라고 선언하는 것이다. 주로 **가장 기본이 되는 정렬 기준**(예: 나이순, ID순)을 정의할 때 쓴다.

```java
public class MyUser implements Comparable<MyUser> {
    private String id;
    private int age;

    @Override
    public int compareTo(MyUser o) {
        // 나이 오름차순 정렬 (내 나이 < 상대 나이 -> -1)
        return this.age < o.age ? -1 : (this.age == o.age ? 0 : 1);
    }
}
```

## 3. Comparator: 별도 정렬 기준 정의

이미 `Comparable`로 기본 정렬이 정의되어 있지만 **다른 기준으로 정렬**하고 싶거나, 내가 수정할 수 없는 클래스를 정렬해야 할 때 사용한다.

```java
public class IdComparator implements Comparator<MyUser> {
    @Override
    public int compare(MyUser o1, MyUser o2) {
        // ID 기준 오름차순 정렬
        return o1.getId().compareTo(o2.getId());
    }
}

// 사용
Arrays.sort(users, new IdComparator());
```

> **💡 정렬의 원리**:
> - 반환값이 **음수(-)**: 첫 번째가 더 작다 -> **그대로 둔다 (오름차순)**
> - 반환값이 **양수(+)**: 첫 번째가 더 크다 -> **자리를 바꾼다**
> - 반환값이 **0**: 같다.
