# 2026-01-24: 순회 (Iterable, Iterator)

- 📚 **강의**: 김영한의 자바 중급2 섹션 11

오늘은 자바의 컬렉션들을 **일관된 방식**으로 순회할 수 있게 해주는 `Iterable`과 `Iterator` 인터페이스에 대해 학습했다. 향상된 `for-each` 문의 동작 원리이기도 하다.

## 1. Iterable과 Iterator

자료구조마다 데이터를 저장하는 방식(배열, 노드 등)은 다르지만, **"순서대로 하나씩 꺼낸다"**라는 동작은 공통적이다. 이 공통점을 추상화한 것이 바로 이 두 인터페이스다.

1.  **Iterable (반복 가능)**:
    -   "나를 순회할 수 있어"라는 표식.
    -   `iterator()` 메서드를 구현하여 **Iterator를 반환**해야 한다.
2.  **Iterator (반복자)**:
    -   실제로 데이터를 하나씩 꺼내는 역할을 하는 객체.
    -   `hasNext()`: 다음 데이터가 있는지 확인.
    -   `next()`: 다음 데이터를 꺼내옴 (커서 이동).

## 2. 향상된 for문 (for-each)의 원리

우리가 흔히 쓰는 `for (int value : list)` 문법은 컴파일러가 다음과 같이 변환하여 실행한다. 즉, **Iterable을 구현한 객체만 for-each에 들어갈 수 있다.**

```java
// 우리가 작성하는 코드
for (Integer value : list) {
    System.out.println(value);
}

// 컴파일러가 변환하는 실제 동작
Iterator<Integer> iterator = list.iterator();
while (iterator.hasNext()) {
    Integer value = iterator.next();
    System.out.println(value);
}
```

## 3. 직접 구현해보기 (MyArray)

직접 만든 자료구조 `MyArray`에서 for-each를 사용하려면 `Iterable`을 구현하면 된다.

```java
// 1. Iterable 구현
public class MyArray implements Iterable<Integer> {
    private int[] numbers;
    
    // ... 생성자 등 생략

    @Override
    public Iterator<Integer> iterator() {
        return new MyArrayIterator(numbers); // 반복자 반환
    }
}

// 2. Iterator 구현
public class MyArrayIterator implements Iterator<Integer> {
    private int[] target;
    private int index = 0;

    @Override
    public boolean hasNext() {
        return index < target.length;
    }

    @Override
    public Integer next() {
        return target[index++];
    }
}
```
